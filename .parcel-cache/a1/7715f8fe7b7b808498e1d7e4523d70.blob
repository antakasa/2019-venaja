"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var playAndPause = function playAndPause() {
  var active = document.querySelector('.swiper-slide-active') || {};
  var prev = document.querySelector('.swiper-slide-prev') || {};
  var next = document.querySelector('.swiper-slide-next') || {};

  if (next && typeof next.querySelector === 'function') {
    var nextVideo = next.querySelector('video');

    if (nextVideo) {
      next.querySelector('video').pause();
    }
  }

  if (typeof prev.querySelector === 'function') {
    var prevVideo = prev.querySelector('video');

    if (prevVideo) {
      prevVideo.pause();

      prevVideo.oncanplay = function () {
        prevVideo.pause();
      };
    }
  }

  if (typeof active.querySelector === 'function') {
    var activeVideo = active.querySelector('video');

    if (activeVideo) {
      var _activeVideo = active.querySelector('video');

      if (parseInt(_activeVideo.readyState) === 4) {
        _activeVideo.play();

        _activeVideo.muted = false;
      } else {
        _activeVideo.oncanplay = function () {
          _activeVideo.muted = false;

          _activeVideo.play();
        };
      }
    }
  }
};

var _default = playAndPause;
exports.default = _default;